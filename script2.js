function playGuessingGame(numToGuess, totalGuesses) {

    let count = 0;
    while (count < totalGuesses) {
        let firstGuess = parseInt(prompt("Enter a number between 1 and 100."))
        if (firstGuess < numToGuess){

        }
    }


}

playGuessingGame(5, 10);